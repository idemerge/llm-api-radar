# Server port
PORT=3001

# Auth credentials (change these before deploying)
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme

# JWT configuration (change JWT_SECRET before deploying)
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=24h
